apiVersion: v1
kind: Service
metadata:
  name: {{ template "authenticationservicewebapi.fullname" . }}
  labels:
    app: {{ template "authenticationservicewebapi.name" . }}
    chart: {{ template "authenticationservicewebapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "authenticationservicewebapi.name" . }}
    release: {{ .Release.Name }}
